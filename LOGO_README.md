# Adding Your Logo

Drop your logo file in the ROOT of this folder named:

  logo.png

The navbar and auth pages will automatically use it.
If logo.png is missing, the site shows the ⚡ icon fallback.

For favicon, drop your icon as:

  favicon.png  (recommended: 64×64 px square)

No code changes needed — just drop the files in.
